# -*- mode: snippet -*-
# name: react-component-elm
# key: elm
# --

import { ReactElement } from "react";
import { I$1Props } from './types'

export const ${1:NAME} = ({ children }: I$1Props): ReactElement => {
	return (
    <div>
		  ${2:}
    </div>
  );
}